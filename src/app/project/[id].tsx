import { router, Stack, useLocalSearchParams } from 'expo-router';
import React from 'react';
import { Pressable, ScrollView, Text, View } from 'react-native';

import { SectionHeading } from '@/components/section-heading';
import { TaskRow } from '@/components/task-row';
import { Glyph } from '@/components/ui/glyph';
import { SurfaceCard } from '@/components/ui/surface-card';
import { COLORS, GUTTER, RADIUS } from '@/constants/theme';
import { useTasks } from '@/context/task-context';
import type { ProjectStatus } from '@/domain/types';

const STATUSES: ProjectStatus[] = ['active', 'paused', 'completed'];

export default function ProjectDetailScreen() {
  const { id } = useLocalSearchParams<{ id: string }>();
  const { projects, tasks, toggleTask, updateProject } = useTasks();
  const project = projects.find((item) => item.id === id);
  const projectTasks = tasks.filter((task) => task.projectId === id || task.project === project?.name).filter((task) => task.status !== 'cancelled');
  const openTasks = projectTasks.filter((task) => !task.completed);
  if (!project) return <View style={{ flex: 1, backgroundColor: COLORS.canvas, alignItems: 'center', justifyContent: 'center' }}><Text selectable style={{ color: COLORS.ink, fontWeight: '800' }}>Project not found.</Text></View>;
  return <><Stack.Screen options={{ title: project.name }} /><ScrollView contentInsetAdjustmentBehavior="automatic" style={{ flex: 1, backgroundColor: COLORS.canvas }} contentContainerStyle={{ padding: GUTTER, paddingTop: 18, paddingBottom: 42, gap: 22 }}><View style={{ backgroundColor: COLORS.contrast, borderRadius: RADIUS.large, padding: 20, gap: 16, borderCurve: 'continuous' }}><View style={{ flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }}><View style={{ width: 43, height: 43, borderRadius: 14, backgroundColor: project.color, alignItems: 'center', justifyContent: 'center' }}><Text style={{ color: COLORS.white, fontSize: 20, fontWeight: '900' }}>{project.name.slice(0, 1)}</Text></View><Pressable accessibilityLabel={`Add task to ${project.name}`} onPress={() => router.push({ pathname: '/add-task', params: { projectId: project.id } })} style={{ width: 44, height: 44, borderRadius: 14, backgroundColor: COLORS.contrastSurface, alignItems: 'center', justifyContent: 'center' }}><Glyph name="plus" size={17} color={COLORS.contrastText} /></Pressable></View><View style={{ gap: 7 }}><Text style={{ color: COLORS.contrastMuted, fontSize: 10, fontWeight: '900', letterSpacing: 1.1 }}>{project.eyebrow}</Text><Text selectable style={{ color: COLORS.contrastText, fontSize: 29, lineHeight: 34, fontWeight: '900' }}>{project.name}</Text><Text selectable style={{ color: COLORS.contrastMuted, fontSize: 13, lineHeight: 19, fontWeight: '600' }}>{project.outcome}</Text></View><View style={{ gap: 8 }}><View style={{ height: 7, borderRadius: 4, backgroundColor: COLORS.contrastSurface, overflow: 'hidden' }}><View style={{ width: `${Math.max(2, project.progress * 100)}%`, height: 7, borderRadius: 4, backgroundColor: project.color }} /></View><Text style={{ color: COLORS.contrastMuted, fontSize: 11, fontWeight: '800' }}>{project.tasksDone} of {project.tasksTotal} complete · {Math.round(project.progress * 100)}%</Text></View></View><View style={{ gap: 10 }}><Text style={{ color: COLORS.ink, fontSize: 13, fontWeight: '900' }}>Project status</Text><View style={{ flexDirection: 'row', gap: 8 }}>{STATUSES.map((status) => <Pressable key={status} accessibilityState={{ selected: project.status === status }} onPress={() => updateProject(project.id, { status })} style={({ pressed }) => [{ flex: 1, minHeight: 44, borderRadius: 14, alignItems: 'center', justifyContent: 'center', backgroundColor: project.status === status ? COLORS.primarySoft : COLORS.surface, borderWidth: 1, borderColor: project.status === status ? COLORS.primary : COLORS.line }, pressed && { opacity: 0.65 }]}><Text style={{ color: project.status === status ? COLORS.primary : COLORS.muted, fontSize: 11, fontWeight: '900', textTransform: 'capitalize' }}>{status}</Text></Pressable>)}</View></View><View style={{ gap: 12 }}><SectionHeading title="Project tasks" subtitle={`${openTasks.length} open · ${projectTasks.length} connected`} action="Add task" onAction={() => router.push({ pathname: '/add-task', params: { projectId: project.id } })} /><SurfaceCard style={{ paddingHorizontal: 16 }}>{projectTasks.length ? projectTasks.map((task) => <TaskRow key={task.id} task={task} onToggle={() => toggleTask(task.id)} onOpen={() => router.push({ pathname: '/task/[id]', params: { id: task.id } })} onStart={() => router.push({ pathname: '/focus', params: { taskId: task.id } })} />) : <View style={{ alignItems: 'center', padding: 24, gap: 7 }}><Text style={{ color: COLORS.ink, fontSize: 14, fontWeight: '900' }}>No tasks yet.</Text><Text style={{ color: COLORS.muted, fontSize: 12, fontWeight: '600' }}>Capture the next action for this direction.</Text></View>}</SurfaceCard></View></ScrollView></>;
}
